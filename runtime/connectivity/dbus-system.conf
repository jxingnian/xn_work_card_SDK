<!-- 固定工牌 rootfs 使用的最小化本机 D-Bus 系统总线配置。 -->
<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-Bus Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>
  <!-- 固定声明为系统总线。 -->
  <type>system</type>

  <!-- 使用固定 rootfs 已存在的低权限 messagebus 用户。 -->
  <user>messagebus</user>

  <!-- D-Bus 自行转入后台，由 S92 服务管理 Socket 和依赖顺序。 -->
  <fork/>

  <!-- PID 和 Socket 只写入每次启动都会清空的内存目录。 -->
  <pidfile>/run/dbus/pid</pidfile>
  <listen>unix:path=/run/dbus/system_bus_socket</listen>

  <!-- 只允许基于本机进程凭据认证，禁止 TCP 和匿名认证。 -->
  <auth>EXTERNAL</auth>

  <!-- 非 root 进程只能连接、接收信号和接收请求的返回值。 -->
  <policy context="default">
    <allow user="*"/>
    <deny own="*"/>
    <deny send_type="method_call"/>
    <allow send_type="signal"/>
    <allow send_requested_reply="true" send_type="method_return"/>
    <allow send_requested_reply="true" send_type="error"/>
    <allow receive_type="method_call"/>
    <allow receive_type="method_return"/>
    <allow receive_type="error"/>
    <allow receive_type="signal"/>
    <allow send_destination="org.freedesktop.DBus"/>
  </policy>

  <!-- 固定固件的蓝牙守护和客户业务均以 root 运行，允许其管理本机 BlueZ 服务。 -->
  <policy user="root">
    <allow own="*"/>
    <allow send_destination="*"/>
    <allow receive_sender="*"/>
  </policy>
</busconfig>
